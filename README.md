# Insight_Challenge_v2
Count words and track median 

Load all files from ./wc_input, and count frequency of each word.
Similarly, count the number of words per line, and track median over all files.
